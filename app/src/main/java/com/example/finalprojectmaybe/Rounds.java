package com.example.finalprojectmaybe;

import android.content.Context;
import android.graphics.BitmapFactory;
import android.media.MediaPlayer;
import android.os.Handler;
import android.util.Log;

//public class Rounds extends RedBalloon{
 //   RedBalloon[]round1 = new RedBalloon[10];
  //  final Handler handler1;
  //  int i=0;

   /* public Rounds(RedBalloon[] round1, Context context) {
        super();
        handler1 = new Handler();

         final Runnable runnable = new Runnable() {
            public void run() {
                while (i < 5) {
                    i++;
                    handler1.postDelayed(this, 5000);
                    round1[i] = new RedBalloon(-Game.getScreenWidth()/50,((int)(Game.getScreenHeight()/2.45)), Game.getScreenWidth()/60,context, BitmapFactory.decodeResource(context.getResources(), R.drawable.red_balloon));
                }
            }
        };
    }*/
//}
